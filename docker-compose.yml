version: '3.8'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    depends_on:
      - redis
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    container_name: redis_cache